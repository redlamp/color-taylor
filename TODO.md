# TODO

- Add more haptics to mobile app
- Fix Channel mode hex drag isolation: in Channel mode, dragging a red handle should only adjust red (currently moves all three). Branch: `feature/hex-channel-drag-fix` off `dev`.
