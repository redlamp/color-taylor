/**
 * Dimensions of the presentation's persistent panel — the single rounded box
 * that every slide's grid, swatch, gradient and intro lettering renders inside.
 *
 * PANEL_W matches the picker's hexagon panel width (SIZE + 40 from
 * hexConstants), which is what lets the last slide scale the real app down to
 * the panel's footprint and grow it from there.
 */
export const PANEL_W = 726;
export const PANEL_H = 320;
