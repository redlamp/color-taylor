*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

28 commits. `TODO.md` removed in favor of GitHub Issues - the tracking model the
project still uses.

Then a run of issue-numbered work: pointer-down conversion for hex dots and
segments (#8), vendor code-splitting via Vite `manualChunks` (#14), `React.memo`
on leaf children and stabilized slider callbacks (#21), a README rewrite off the
Vite scaffold (#20), and drag haptics (#7). A `deploy:dev` script for a `/dev/`
subpath preview landed at the end.

Every one went through a `feature/*` branch and a PR.
