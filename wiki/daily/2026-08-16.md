Reviewed Figma's BYFP (Build Your First Plugin) course series against the plugin work. Nothing already done is contradicted; takeaways were release-plan confirmations — support contact is a required publish field, comments default on, and the Publish modal itself has a Generate ID button as an alternative to creating the plugin first. One flag: the `accessibility` tag in `figma/brand/listing.md` is ahead of the keyboard-operability gap noted in [[plan-figma-plugin-release]].

Acted on the course's one tooling point: the sandbox is now typechecked. `@figma/plugin-typings` added, `figma/tsconfig.json` runs `checkJs` over the JSDoc-annotated `code.js` (still plain JS, no build step), and the UI↔sandbox bridge protocol now lives once in `figma/messages.ts`, referenced from `code.js`, `main.tsx` and `swatch-store.ts` — a message shape changed on one side alone now fails `bun run typecheck`, which CI already runs. Gotcha for later: `figma/tsconfig.json` shadows the root one for Vite's `figma/ui` build, so its `jsx`/`useDefineForClassFields` flags are mirrored from the root config and must stay in sync. All gates green (typecheck, lint, build:figma byte-profile unchanged, 21 e2e).

Follow-up: `@figma/eslint-plugin-figma-plugins` wired into the existing `figma/code.js` block of `eslint.config.js` — it turned out to be trivial *because* the sandbox tsconfig already existed (its rules are type-aware and read types from `figma/tsconfig.json`). Verified the rules actually fire by planting a deprecated `figma.getNodeById` call and watching `ban-deprecated-sync-methods` flag it. `code.js` passes clean; the earlier assessment that this wasn't worth the setup is superseded — the setup cost had already been paid.
