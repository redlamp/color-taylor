*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

25 commits, nearly all responsive layout.

The root max-width shrunk to the sum of the panels, the sliders panel made
shrinkable, and the hex column set to shrink proportionally with it in two-column
mode. `SBBox` fills available width at a 17:10 aspect. `NamedColorMatch` got
container-query compaction rather than viewport breakpoints.

Nine of the commits are EquationsPanel column ratios - 1/1.2/1/0.8, then Variables
to 1.1fr, then 1.2/1.2/1/0.6, then BL to 0.75fr - and finally lowering the
four-column break to 640px so columns shrink further before stacking.
