*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

70 commits, the largest day in the project. Two halves.

**Morning: app features.** localStorage persistence (theme, color, recents),
keyboard shortcuts on the steppers (arrows +/-1, Shift +/-10), invalid-hex
feedback, undo/redo on Ctrl+Z with a 400ms tween, a Learn section, and the
Conversions panel (Inverse, Complementary, Lighten, Darken).

**Afternoon: presentation mode.** Hash routing, a slide system, then the retro
Mac Monitors panel - the 16-color CLUT4 and 256-color CLUT8 palettes laid out
exactly as the Mac control panel showed them, with cells that tween between
layouts by identity rather than by index.

Most of the day's commit count is that animation getting tuned: matched cells
tween first and new cells fade in after, then at 50% overlap, then 70%; timings
went 200/400ms, then 400/600ms; easing settled on the Material standard curve;
the stagger was inverted so bright colors arrive first. A generation counter was
needed to stop stale cleanup overwriting in-flight transitions.

Also here: sine-wave animation of H/S/B with per-channel periods, later replaced
by a linear hue loop, and a Black R Y G C B M White keyframe cycle over 16s.
