*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

6 commits. Static build support, and the sRGB/Linear color space toggle - one
switch that changes hex output, slider gradients and the brightness bar together.

HTML color names merged into the Hex panel rather than living in their own.
Z-index fight between the color labels and the hex SVG vectors, settled by
putting the labels behind.
