App-side work split off from the redlamp-videos presentation session. The presentation branch moved to its own worktree (`C:\workspace\color-taylor-prez`, `prez/cut-03`, server on :5173); the main tree is on `dev`.

- #103 merged: two-column layout starts at 900px; slider tracks center on their steppers.
- `prez/cut-03` rebased onto dev (15 ahead); draft PR #104 replaces #102, closed. Stays draft until the walkthrough has a production entry point.
- #106 open: the hexagon's brightness pill clamps to the card instead of overflowing it between 900 and 1088px (`translate` with `cqw`, no resize observer).
- #105 filed: hex readout stacks above the bank toggles when the editor card is narrow. Then widened into [[plan-narrow-widths]], Taylor's staged reflow of every card down to 240px.
- Re-evaluated the 900px breakpoint on `fix/column-breakpoint`: **it moves to 800px.** #103's 320px floor does protect the editor — measured, that column sits pinned at exactly 320px from 760 to 880 and nothing in it leaves the card, wraps or scrolls, so it is no longer what binds. The hexagon column is. Swept 12 hues × 9 brightnesses per width: the hue badge and the brightness pill are fixed-size chrome on a shrinking hexagon and they collide at 794px (0.6px, h0/b40), 2.3px at 784, worse below; clean at 796 and up. 800 is that rounded up. md's 768 is 28px the wrong side, so the Tailwind step is not available. Columns still measure flush (Δ 0.0) at every width from 1100 down to 800.
- Working mode from today: main session plans, briefs and reviews; builds go to subagents.
