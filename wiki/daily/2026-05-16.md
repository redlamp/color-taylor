*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

54 commits after a six-week gap. Three streams.

**TypeScript migration**, in stages - scaffolding in mixed JS/TS mode, then color
math utils, then shadcn primitives, then components and presentation, then
`allowJs` dropped. `CLAUDE.md` was added at the start of the day, which is where
the repo conventions begin.

**Mobile.** Touch drag on the hexagon, scaled hex color labels, a constrained
layout width, and the rule that narrowing shrinks the hexagon rather than the
sliders. Page margins were tightened three times in a row.

**The Saved panel.** "Recent Colors" became "Recent" and Saved appeared beside
it, then grew most of a day's work on its own: a sort cycle, a User sort that
preserves manual arrangement, drag-to-reorder, long-press to drag on touch,
drag-out-to-delete with a poof animation, and FLIP tweens that track colors by
identity rather than slot index. Drop-on-cell swaps, drop-between inserts.

Each of those actions got a synth sound - a paper crumple on delete, a stamp on
save, a soft woosh on sort - with per-trigger random variation so repeats don't
sound mechanical. See [[decision-audio-as-optional-module]] for where that ended
up.
