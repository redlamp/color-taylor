*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

48 commits. Opens by reverting both haptics PRs from the 18th, then re-landing
haptics as discrete taps on key actions rather than continuous drag feedback.

The bulk of the day is issue #36 - a React-correctness sweep. Refs written during
render, `useRef` below an early return, effect bodies doing what cleanup should,
components defined inside another component's render, memo deps that didn't match
what was inferred. Ten commits, each one narrow.

Alongside it: a lint cleanup pass (#15), the dead `circle` literal removed from
the dragMode union (#17), the synth split into a lazy engine plus sync config
(#22), the Handles toggle removed in favor of unified hex drag, and the Intro
section put behind `VITE_INTRO_ENABLED`.
