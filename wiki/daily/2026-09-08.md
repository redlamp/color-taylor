Play ring on the way, not on arrival: while Saved or Recent plays, the selection ring moves to the
swatch the colour is travelling to as each transition starts. It went through the same pending mark
a swatch click uses, with the lifetime stretched to the step's transition - a click's tween is a
second, a slow Play Speed's transition can be three. PR #92.

Taylor asked why the site had not picked up play. It had, since Saturday's deploy; the Swatches
panel starts closed and its play button only shows while it is open, the same rule as Sort,
Defaults and Clear. Worth a second look at whether the panel's own control should stay visible
when it is collapsed.

Then the real ask: the span between "ship it" and the site. Seven minutes of it was the merge
commit re-running the e2e the pull request had just run. [[decision-fast-deploy-gate]]: the deploy
waits for lint, types and the unit tests; the Playwright suite runs beside it and gates the PR.
No CI on dev pushes either - the same commit was being tested twice on two runners.

Both labs baked into single files with the existing bundler, `dist-lab/cube-bench.html` and
`dist-lab/spectrum-bench.html`, for opening from disk; the dev server serves them at
`/lab/cube.html` and `/lab/spectrum.html`.
