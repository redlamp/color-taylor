*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

19 commits. Presentation mode merged to main.

Typography churn - DM Mono, then Martian Mono, then Share Tech Mono - with the
Equations panel column ratios retuned after each. Unused font packages and
`next-themes` dropped at the end.

A cleanup pass closed the day: top-level ErrorBoundary, shared palette data and a
contrast-text utility extracted, `!important` CSS overrides replaced with
Tailwind theme tokens, commented-out code removed, PresentationShell lazy-loaded
for a smaller initial bundle. Two real bugs fixed: a stale closure in the theme
context, and `isUndoRedoing` not resetting when an animation was interrupted.
