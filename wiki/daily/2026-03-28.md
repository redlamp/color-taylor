*Reconstructed from git history on 2026-08-03. Records what the commits show, not what was discussed.*

14 commits. Day one.

The first commit is already a working color picker with an HSB/HSL hexagon - the
hexagon was the idea from the start, not something arrived at later. By the end
of the day it had draggable dots, a brightness slider, a live Equations panel
showing the HSB/HSL formulas color-coded, and named CSS color matching with an
adjustable distance threshold.

Notable that the refactors landed the same day as the features: `useDrag`
extracted out of ColorSlider/HSlider/SBBox, ColorHexagon split into
sub-components, a ThemeContext replacing direct DOM queries for dark mode, and an
rAF-debounced canvas redraw. The shape the project still has - one hexagon
component with sub-components under it, one shared drag hook - was set here.

ARIA labels and roles went in on day one too.
