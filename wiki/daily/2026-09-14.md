Presentation prep day. The app side shipped the last of the picker polish into `dev`, then spent the afternoon and evening answering the presentation side's requests against `prez/cut-03`. Every build went to a subagent with a verification budget; the main session planned, briefed and reviewed.

## Into dev

- #121: the SB box carries axis labels, "Saturation" bottom right and "Brightness" (or "Lightness") bottom left, vertical; they fade within 24px of the pointer and hide during a drag. Taylor tried centred labels first and moved them to the corners.
- #122: tooltips. Named-colour matches say "Move to #RRGGBB", the preview swatch and the copyable results say "Click to copy", all in the plain top style so the color editor's and the swatch's read the same way.
- #123: the hover dot and the HTML tag markers sit at the colour's own brightness on the current shape (`pointForColor`, the inverse of `colorAtPoint`), so mediumvioletred no longer lands outside the rim. The vertex letters and the 0/50/100 markers went to text-sm.
- #124: a `?` beside the Equations Hex title explains base 16 on hover. Four lines at text-base, Taylor's wording, three spaces after each comma in the number line.
- #125: every HTML colour on the hexagon's field, faded by its distance in brightness from the current value. Started as a trial (`trial/all-html-markers`); Taylor kept it. [[decision-all-html-colours-on-the-field]].
- #130 open: the Equations grid on `leading-tight` with half the row gap, 281px to 240px at 1920×1080. Taylor's pick of three measured options.

## Into prez/cut-03, for the walkthrough

All requested through the presentation session, which merged each one. [[plan-walkthrough-entry]] has the built list; [[presentation-system]] describes what their side built.

- #126 (theirs): the `?present=` spec probes `/__notes/<cut>` for JSON to learn whether the dev tools are served; the webcam element may carry a blob src.
- #127: the Presentation button leaves the About panel open. Cut 04's first beat underlines the title inside it and the runner clicks Get Started itself at the top of beat two. The button still creates the voice and webcam elements and calls `play()` on the click; it only writes the seen flag now.
- #128 then #131: the Presentation caption, "four minutes" to "~6 min" to "~5.5 min", following the cut. #131 also changes the panel's line to "Play with different color models" - the app says models, not modes.
- #129: the header's Intro button is hidden unless the URL carries `?intro` (or `?intro=1`). This replaces the `VITE_INTRO_ENABLED` build flag, which leaves both env files, the `deploy:dev` script and the docs that described it.
- A question from their side, answered by measurement: the runner's synthetic click on `#about-close` did nothing because it was dispatched with `bubbles: false`. React listens at the root, so a click that does not bubble never reaches the handler; a bubbling one closes the panel. No host change.

## Also

- Asked and answered: the app uses no GSAP. Every tween is a hand-rolled rAF loop - quadratic ease-in-out for colour (`ColorPicker.tsx`), cubic ease-out for the demo's motion, a quintic smoothstep for the ghost cursor in `drive.ts`.
- HSB or HSL for the presentation: HSB, with one beat on the HSL toggle. "Designers pick in HSB; the web writes HSL; they're two slices of the same cube."
- The next morning (09-15) #104 merged: the whole walkthrough is on `dev` at 3b85f9d. The presentation side's summary of its system went into [[presentation-system]] the same morning.
